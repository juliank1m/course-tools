// Learn more: https://github.com/testing-library/jest-dom
require('@testing-library/jest-dom')

// Note: You may see console.error messages during tests - these are EXPECTED!
// They come from error handling code being tested (e.g., missing API keys, invalid responses).
// The tests verify that errors are logged and handled correctly.
// To suppress them, uncomment the following:
// const originalError = console.error;
// beforeAll(() => {
//   console.error = jest.fn();
// });
// afterAll(() => {
//   console.error = originalError;
// });

